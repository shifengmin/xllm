include(cc_library)

cc_library(
  NAME
    platform_vmm
  HDRS
    submitter/common.h
    submitter/manager.h
    submitter/submitter.h
    submitter/worker.h
    vmm_api.h
  SRCS
    submitter/manager.cpp
    submitter/submitter.cpp
    submitter/worker.cpp
    vmm_api.cpp
  DEPS
    torch
    $<$<BOOL:${USE_NPU}>:torch_npu>
    $<$<BOOL:${USE_NPU}>:ascendcl>
    $<$<BOOL:${USE_MLU}>:torch_mlu>
    $<$<BOOL:${USE_MLU}>:cnrt>
    $<$<BOOL:${USE_MLU}>:cndrv>
    $<$<OR:$<BOOL:${USE_CUDA}>,$<BOOL:${USE_ILU}>>:cuda>
    $<$<OR:$<BOOL:${USE_CUDA}>,$<BOOL:${USE_ILU}>>:cudart>
    $<$<BOOL:${USE_MUSA}>:musa>
    $<$<BOOL:${USE_MUSA}>:musart>
)